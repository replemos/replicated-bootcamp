# k3d Local Dev Environment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace `docker compose up` with a k3d-based hybrid dev environment — postgres, redis, and the Replicated SDK run in a local k3d cluster; Next.js continues to run locally via `npm run dev`.

**Architecture:** Add an `app.enabled` toggle to the Helm chart so the app Deployment can be suppressed in dev. A new `values.dev.yaml` override file disables the app and sets a dummy `nextauth.secret`. The existing `images.emosbaugh.be` proxy + `global.replicated.dockerconfigjson` auth pattern (same as CMX testing) handles image pulls — no registry overrides needed. A new `docs/testing-locally.md` documents the full setup flow.

**Tech Stack:** Helm 3, k3d, kubectl, Replicated SDK integration mode

---

### Task 1: Add `app.enabled` toggle to the Helm chart

**Files:**
- Modify: `deploy/charts/values.yaml`
- Modify: `deploy/charts/templates/deployment.yaml`
- Modify: `deploy/charts/values.schema.json`

- [ ] **Step 1: Add `app.enabled: true` to values.yaml**

Open `deploy/charts/values.yaml`. After the `replicaCount` line, add:

```yaml
app:
  enabled: true
```

The surrounding context should look like:

```yaml
replicaCount: 1

app:
  enabled: true

# Pull secret created automatically by the Replicated SDK at install time
```

- [ ] **Step 2: Wrap deployment.yaml in the conditional**

Open `deploy/charts/templates/deployment.yaml`. Add `{{- if .Values.app.enabled }}` as the very first line and `{{- end }}` as the very last line:

```yaml
{{- if .Values.app.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "playball-exe.fullname" . }}
  labels:
    {{- include "playball-exe.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "playball-exe.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "playball-exe.selectorLabels" . | nindent 6 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-db
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - node
            - -e
            - |
              const net = require('net');
              const u = new URL(process.env.DATABASE_URL);
              const host = u.hostname, port = parseInt(u.port || '5432');
              (function check() {
                const s = net.createConnection(port, host);
                s.on('connect', () => { console.log('DB ready'); s.end(); process.exit(0); });
                s.on('error', () => { console.log('Waiting for DB...'); setTimeout(check, 2000); });
              })();
          envFrom:
            - secretRef:
                name: {{ include "playball-exe.fullname" . }}
        - name: migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - node
            - node_modules/prisma/build/index.js
            - migrate
            - deploy
          envFrom:
            - secretRef:
                name: {{ include "playball-exe.fullname" . }}
          resources:
            {{- toYaml .Values.initContainers.resources | nindent 12 }}
        - name: seed
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - node
            - prisma/seed.js
          envFrom:
            - secretRef:
                name: {{ include "playball-exe.fullname" . }}
          resources:
            {{- toYaml .Values.initContainers.resources | nindent 12 }}
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "playball-exe.fullname" . }}
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
{{- end }}
```

- [ ] **Step 3: Add `app` property to values.schema.json**

Open `deploy/charts/values.schema.json`. Find the `"imagePullSecrets"` property block (currently the last entry inside `"properties"`). Add the `"app"` entry immediately before it, with a trailing comma:

```json
    "app": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        }
      }
    },
    "imagePullSecrets": {
```

- [ ] **Step 4: Verify helm lint passes with default values**

```bash
helm lint deploy/charts --set nextauth.secret=test
```

Expected: `1 chart(s) linted, 0 chart(s) failed`

- [ ] **Step 5: Verify `app.enabled=false` suppresses the app Deployment**

```bash
helm template playball deploy/charts --set nextauth.secret=test --set app.enabled=false | grep 'kind: Deployment'
```

Expected: only the SDK Deployment appears (no app Deployment):

```
kind: Deployment
```

(one line — the SDK subchart's Deployment only)

- [ ] **Step 6: Verify `app.enabled=true` (default) still renders the app Deployment**

```bash
helm template playball deploy/charts --set nextauth.secret=test | grep 'kind: Deployment'
```

Expected: two lines (SDK + app):

```
kind: Deployment
kind: Deployment
```

- [ ] **Step 7: Commit**

```bash
git add deploy/charts/values.yaml deploy/charts/templates/deployment.yaml deploy/charts/values.schema.json
git commit -m "feat: add app.enabled toggle to suppress app Deployment in dev"
```

---

### Task 2: Create `values.dev.yaml`

**Files:**
- Create: `deploy/charts/values.dev.yaml`

- [ ] **Step 1: Create the file**

```yaml
# Dev override — runs only postgres, redis, and the Replicated SDK in k3d.
# The Next.js app runs locally via `npm run dev` instead.
# See docs/testing-locally.md for full setup instructions.

app:
  enabled: false

nextauth:
  secret: dev
  url: http://localhost:3000
```

- [ ] **Step 2: Verify helm lint passes with dev values**

```bash
helm lint deploy/charts -f deploy/charts/values.dev.yaml
```

Expected: `1 chart(s) linted, 0 chart(s) failed`

- [ ] **Step 3: Verify only the SDK Deployment is rendered**

```bash
helm template playball deploy/charts -f deploy/charts/values.dev.yaml | grep 'kind: Deployment'
```

Expected: one line (SDK only, app suppressed):

```
kind: Deployment
```

- [ ] **Step 4: Commit**

```bash
git add deploy/charts/values.dev.yaml
git commit -m "feat: add values.dev.yaml for k3d hybrid dev environment"
```

---

### Task 3: Update `.env.example`

**Files:**
- Modify: `.env.example`

- [ ] **Step 1: Add `REPLICATED_SDK_URL`**

Open `.env.example`. The file currently ends with `REDIS_URL`. Append the SDK URL:

```
DATABASE_URL="postgresql://baseball:baseball@localhost:5432/baseball"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
REDIS_URL="redis://localhost:6379"
REPLICATED_SDK_URL="http://localhost:3001"
```

- [ ] **Step 2: Run tests to confirm nothing broke**

```bash
npm test
```

Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add .env.example
git commit -m "feat: add REPLICATED_SDK_URL to .env.example for k3d dev"
```

---

### Task 4: Write `docs/testing-locally.md`

**Files:**
- Create: `docs/testing-locally.md`

- [ ] **Step 1: Create the doc**

```markdown
# Testing Locally with k3d

Use [k3d](https://k3d.io) to run postgres, redis, and the Replicated SDK in a local Kubernetes cluster while developing Next.js locally with `npm run dev`.

## Prerequisites

- [`k3d`](https://k3d.io/#installation) installed
- `kubectl` installed
- `helm` installed

## Set Your License ID

The Replicated SDK requires a license to start. Get a dev license ID from the [Vendor Portal](https://vendor.replicated.com) and export it once at the start of your session:

```bash
export REPLICATED_LICENSE_ID=<your-license-id>
```

> **Note for AI assistants:** The Bash tool does not inherit shell environment variables. When running `helm install`, pass the license ID literally rather than relying on `${REPLICATED_LICENSE_ID}` expansion.

## Create a Cluster

```bash
k3d cluster create playball-dev
```

Verify it's ready:

```bash
kubectl get nodes
```

## Install the Helm Chart

Images are proxied through `images.emosbaugh.be`, which requires authentication. Generate the pull secret config using your license ID:

```bash
DOCKER_CONFIG=$(echo -n "{\"auths\":{\"images.emosbaugh.be\":{\"auth\":\"$(echo -n "${REPLICATED_LICENSE_ID}:${REPLICATED_LICENSE_ID}" | base64)\"}}}" | base64)
```

Install with the dev values override — this disables the app Deployment and runs only postgres, redis, and the Replicated SDK:

```bash
helm install playball deploy/charts \
  -f deploy/charts/values.dev.yaml \
  --set replicated.integration.licenseID="${REPLICATED_LICENSE_ID}" \
  --set global.replicated.dockerconfigjson="${DOCKER_CONFIG}" \
  --wait --timeout 5m
```

Verify all pods are running:

```bash
kubectl get pods
```

## Port-Forward Services

Forward all three services to localhost (run these in the background):

```bash
kubectl port-forward svc/playball-postgresql 5432:5432 &
kubectl port-forward svc/playball-redis-master 6379:6379 &
kubectl port-forward svc/playball-exe-sdk 3001:3000 &
```

> The SDK is forwarded to port **3001** (not 3000) to avoid conflicting with the Next.js dev server.

## Configure the App

```bash
cp .env.example .env.local
```

Open `.env.local` and replace the `NEXTAUTH_SECRET` placeholder with a generated secret:

```bash
openssl rand -base64 32
```

The file already includes `REPLICATED_SDK_URL=http://localhost:3001` pointing to the port-forwarded SDK.

## Run Migrations

```bash
npx prisma migrate dev
npx prisma db seed
```

## Start the Dev Server

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

## Clean Up

Stop port-forwarding and delete the cluster when done:

```bash
kill %1 %2 %3
k3d cluster delete playball-dev
```
```

- [ ] **Step 2: Commit**

```bash
git add docs/testing-locally.md
git commit -m "docs: add testing-locally.md for k3d hybrid dev environment"
```

---

### Task 5: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace the "Start the database" section**

Find the current step 1:

```markdown
### 1. Start the database

```bash
docker compose up -d
```
```

Replace with:

```markdown
### 1. Start infrastructure

For full local development with the Replicated SDK, use k3d — see [Testing Locally with k3d](docs/testing-locally.md) for setup instructions.

For quick development without the SDK (no license required):

```bash
docker compose up -d
```
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: update README to reference k3d dev env"
```
